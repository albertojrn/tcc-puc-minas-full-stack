import { styled } from '@mui/material'

export const FileInput = styled('input')`
  bottom: 0;
  clip-path: inset(50%);
  clip: rect(0 0 0 0);
  height: 1;
  left: 0;
  overflow: hidden;
  position: absolute;
  white-space: nowrap;
  width: 1;
`
